# 8004表述注册到zookeeper府二区的支付服务提供者端口号
server:
  port: 80

# 服务别名---zookeeper注册中心名称
spring:
  application:
    name: consul-consumer-order
  cloud:
    zookeeper:
      connect-string: 192.168.239.131:2181

